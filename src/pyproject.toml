[project]
name = "src"
version = "0.1.0"
description = "POC for submitting a Quidlo Timesheets entry with Playwright"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "playwright>=1.54.0",
]
